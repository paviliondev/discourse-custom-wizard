{{#if hasMultiple}}
  {{combo-box
    value=connector
    content=connectors
    onChange=(action (mut connector))}}
{{else}}
  <span class="connector-single">
    {{connectorLabel}}
  </span>
{{/if}}